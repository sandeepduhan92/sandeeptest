---
 - hosts: test
   vars:
      zabbix_agent_download_url: 'http://54.255.184.29/setup/upgrade.sh'
   tasks:
 #      - name: Download Zabbix agent from remote
  #       get_url:
   #            url: "{{zabbix_agent_download_url}}"
    #           dest: /tmp/sandeep
              
                 
       - name: install Zabbix agent on host
         shell: command sh /tmp/sandeep/upgrade.sh

